name: "cpu resource test"
version: 1
steps:
- "assert 1 large node"
- "assert 2 small nodes"
- "assert 0 pods"

- "create 3 4-cpu pods"
- "assert 2 4-cpu pods are Running"
- "assert 1 4-cpu pod is Pending"

- "change 1 4-cpu pod from Running to Succeeded"
- "assert 2 4-cpu pods are Running"
- "assert 1 4-cpu pod is Succeeded"
- "assert 0 4-cpu pods are Pending"

- "delete 3 4-cpu pods"
- "assert 0 pods"
