
apiVersion: v1
kind: Pod
metadata:
  name: 2-cpu-4-gang-pod-4
  annotations:
    scheduling.k8s.io/group-name: "pg2"
  labels:
    np.class: 2-cpu-4-gang
    np.runDuration: 5s
    np.terminalPhase: Succeeded
spec:
  schedulerName: kube-nnpt-scheduler
  tolerations:
    - key: node.kubernetes.io/unreachable
      effect: NoSchedule
  containers:
  - name: test1
    image: ubuntu:latest
    imagePullPolicy: IfNotPresent
    command: [ "/bin/bash", "-c", "--" ]
    args: [ "while true; do sleep 30; done;" ]
    resources:
      requests:
        cpu: 2
        memory: 128Mi
  restartPolicy: Never